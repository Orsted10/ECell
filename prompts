Since you've connected Antigravity directly to the **Framer project**, you should **not** ask it to redesign the UI. Otherwise, it'll fight the template and you'll lose the premium look you paid for.

Instead, make the template the **source of truth** and tell Antigravity to adapt it.

---

# MASTER ANTIGRAVITY PROMPT (Framer Template Mode)

```text
You are working INSIDE an existing premium Framer template.

The Framer template is the DESIGN SOURCE OF TRUTH.

Your job is NOT to redesign the website.

Your job is to transform the template into the official Entrepreneurship Cell website for Chandigarh University Uttar Pradesh while preserving the original design language.

ABSOLUTE RULES

Never redesign the template.

Never replace the visual identity.

Never create a different design language.

Never introduce a second design system.

Never make the website look AI generated.

Never make the website look like a startup landing page.

Never create generic hero sections.

Never create random cards or layouts.

Instead...

Study every section of the template.

Understand why each layout exists.

Reuse every layout.

Reuse every spacing system.

Reuse every typography scale.

Reuse every animation.

Reuse every interaction.

Reuse every transition.

Reuse every component.

Reuse every visual hierarchy.

Reuse every responsive behavior.

Reuse every motion curve.

Reuse every lighting effect.

Reuse every image treatment.

Reuse every hover animation.

Reuse every scroll animation.

Reuse every design token.

Reuse every border radius.

Reuse every shadow.

Reuse every padding and margin system.

The goal is that someone familiar with the original template immediately recognizes it, while simultaneously believing it was originally designed specifically for Chandigarh University's Entrepreneurship Cell.

Only modify:

• Content
• Copywriting
• Images
• Icons
• Branding
• Colors only where branding requires
• Navigation labels
• CTA text
• Data

If a new section is required:

DO NOT invent a new style.

Instead clone the closest existing section in the template and adapt it.

Every newly created component must feel like it shipped with the original template.

The final result must look like the original template creator designed the E-Cell website themselves.
```

---

# WEBSITE TRANSFORMATION

```text
Transform this premium Framer template into the official Entrepreneurship Cell website.

The template already contains an excellent design system.

Respect it.

Preserve its premium feel.

Map the template sections into an E-Cell narrative.

The story should become

Dream

↓

Learn

↓

Build

↓

Launch

↓

Grow

↓

Join

without changing the template's visual language.

If an existing section can communicate the same idea, reuse it instead of replacing it.
```

---

# HERO

```text
Do NOT redesign the hero.

Keep the exact layout.

Keep the typography style.

Keep the animation system.

Keep the motion language.

Keep the spacing.

Only replace the content.

Transform it into an Entrepreneurship Cell hero.

Possible headline

Where Ideas Become Startups.

or

We Build Founders.

Replace imagery with authentic entrepreneurship, startup, innovation and campus visuals that match the template's aesthetic.

Keep every interaction exactly as originally designed.
```

---

# CONTENT REPLACEMENT RULE

```text
For every section in the template:

First determine what purpose it serves.

Then replace only the content while preserving the structure.

Example

Testimonials
↓

Founder Success Stories

Features
↓

What E-Cell Offers

Services
↓

Programs

Portfolio
↓

Startup Showcase

Clients
↓

Industry Partners

Case Studies
↓

Student Startup Journeys

Pricing
↓

Membership Benefits

Blog
↓

Insights & Events

The layout should remain recognizable.
```

---

# NEW SECTIONS

```text
If the E-Cell requires content that the template doesn't include:

Never invent a new visual style.

Instead:

Duplicate the most similar existing section.

Modify it.

Keep identical spacing.

Keep identical typography.

Keep identical animations.

Keep identical responsiveness.

Keep identical motion.

Keep identical interaction patterns.

The new section must look native to the template.
```

---

# DESIGN SYSTEM

```text
The existing Framer template is the design system.

Do not create another one.

Respect

Typography

Spacing

Grid

Columns

Cards

Buttons

Radius

Shadows

Animations

Motion timing

Hover states

Page transitions

Image masks

Lighting

Gradients

Depth

Everything must remain internally consistent.

Never mix styles.
```

---

# MOTION

```text
Reuse every motion pattern already present.

Do not replace animations.

Only redirect them to the new content.

If a card slides in originally,

the E-Cell version should slide in identically.

If images reveal through masks,

preserve the same reveal.

If the template has premium transitions,

keep them exactly.

Motion consistency is more important than adding new animations.
```

---

# COMPONENT REUSE

```text
Before creating anything new,

search the existing template.

If a similar component already exists,

reuse it.

Modify it.

Extend it.

Never rebuild it.

The objective is maximum component reuse.
```

---

# FINAL QUALITY CHECK

```text
Before every change ask yourself:

Does this still feel like the original template?

If not,

don't do it.

The user bought this template because of its design.

Your responsibility is to preserve that design while transforming its content into a world-class Entrepreneurship Cell experience.

The final website should look as if the original Framer template was always intended to be an E-Cell website.
```

---

## One extra instruction I'd add (this is the secret sauce)

Append this at the end:

```text
Before making ANY UI modification:

1. Analyze the existing template completely.
2. Identify its design principles.
3. Document them internally.
4. Follow those principles for every future change.
5. Do not make a single visual decision that conflicts with the original template.

Your success is measured by how little the design language changes while how completely the content transforms.
```

This prompt shifts Antigravity from **"designer"** to **"creative adapter."** It will leverage the Framer template's layouts, components, animations, and visual language almost entirely, producing a site that feels custom while retaining the premium craftsmanship of the original template.
